# Local development (docker compose up -d)
DATABASE_URL=postgres://batti:batti_local_dev@localhost:5433/batti?sslmode=disable

# Production (Supabase) — filled in at Phase 3
# DATABASE_URL=postgres://postgres:<password>@db.<project-ref>.supabase.co:5432/postgres?sslmode=require
